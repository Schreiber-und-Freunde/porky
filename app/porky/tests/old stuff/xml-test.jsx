#include DBUI-Connector-2.0.jsx;//localhost/xml-json.php?language=en&id=8508alert(    getWebData("ru", "8508"));function getWebData(language, id){    var reply = "";    var conn = new Socket;    conn.timeout = 600000;    if(conn.open ("localhost:80", "UTF-8")){        // send a HTTP GET request        // standard including header information        // var result = conn.write ("GET /xml-json.php?language=" + language + "&id=" + id + " HTTP/1.0\n\n");// this forces empty header in the responsevar result = conn.write ("GET /xml-json.php?language=" + language + "&id=" + id + "\n");        // and read the server’s reply        reply = conn.read(99999999999);        var close = conn.close();    }else{        alert("Warning: No connection to IFG project API!");    }    if(reply.length < 1){        //alert("Empty result!", "IFG");    }    if(reply.length > 0){        return reply;    }}