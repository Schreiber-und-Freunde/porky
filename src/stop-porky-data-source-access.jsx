//@include ./lib/base64/base64-encode-decode.jsvar conn = new Socket;conn.timeout = 600000;if (conn.open ("127.0.0.1:6789", "UTF-8")) {    // send a HTTP GET request    var result = conn.write ("GET /" + "cXVpdCBwb3JreQo=" + " HTTP/1.0\n\n");    // and read the server’s reply    reply = conn.read(99999999999);    var close = conn.close();    if(reply.length < 1){        alert("porky\nquitting data source access interface...");    }else if(reply.length > 0){        alert("Warning:\nSomething went wrong while quitting porky data source access interface.\n\nporky response: " + Base64.decode(reply));    }}else{    //alert("Cannot stop porky db access!");}