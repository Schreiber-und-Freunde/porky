#include ../bin/porky.jsx;alert("Attention\nIn InDesign go to View/Structure/Show Structure or hit Command-Alt-1");alert("This script will create a text box with several text objects inside.\n\nThose will get attributes as you can see in the structure panel.\n\nVia these attributes you can update/sync their content (here by requesting a database)!!!");var myTextframe = addFrame(15, 15, 100, 50, "");// creating a textframe and appending some static text var myTextObject1 = appendToFrame(myTextframe, "Hey");// tagging the text with attributes so it can be updated/synchronizedvar mySyncObject1 = tagThis(myTextObject1, "/salutation.jsx", "prepared");appendToFrame(myTextframe, "\r");var myTextObject2 = appendToFrame(myTextframe, "Oliver");var mySyncObject2 = tagThis(myTextObject2, "/name.jsx", "prepared");appendToFrame(myTextframe, "\r\r");var myTextObject3 = appendToFrame(myTextframe, "Enjoy your holidays!");var mySyncObject3 = tagThis(myTextObject3, "/slogan.jsx", "prepared");