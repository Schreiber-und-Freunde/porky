//@include ../bin/porky.jsx;/*    Select an XML element in InDesign's XML structure and run this script.    It will sync the element's content by using the xml attributes name/value pair which is inside the element's tag.    Global object settings.sync.identifier will be set by running this function, global object settings.sync.scriptFolder will be used for finding the sync script*/// Where's the sync script? Here it is...var thisParentFolder = File($.fileName).parent;var thisRootFolder = File("~").fsName;var thisAbsolutePath = unescape( thisParentFolder.toString().replace("~", thisRootFolder) );    thisAbsolutePath = thisAbsolutePath.replace(/\\/g, "/");settings.sync.scriptFolder = thisAbsolutePath + "/syncScripts";// What to sync? Let's sync this XML elementif(app.selection[0] == "[object XMLElement]"){    alert("syncing content via a script from here:\n\n" + settings.sync.scriptFolder);    var myXMLElement = syncXMLElement(app.selection[0]);}else{        alert("Check this\nIn InDesign go to View/Structure/Show Structure or hit Command-Alt-1,\n\nthen go and select a single XML element you wish to sync its content for from the structure panel on the left.\n\nThen re-run this script!");    }