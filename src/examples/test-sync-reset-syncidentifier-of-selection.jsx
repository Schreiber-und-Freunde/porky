//@include ../bin/porky.jsx;// set XML attribute "syncIdentifier" in selection e.g. to "1"if(app.selection[0]){    var mySyncIdentifier = prompt ("enter a unique database ID\n" + "which will be reused for syncing (e.g. 1, 2 or 3): ", "1");    if(mySyncIdentifier){        // forward every selected object to recursiveSetSyncIdentifier()        recursiveSetSyncIdentifier(app.selection, mySyncIdentifier);    }}else{    alert("Error\nNothing selected!");}