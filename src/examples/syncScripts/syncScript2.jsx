// Libraries should already be loaded by the calling script// Global data source connection objectsettings.dataSource.type = "XML";settings.dataSource.server = "";settings.dataSource.name = "http://weather.yahooapis.com/forecastrss?w=5" + settings.sync.identifier + "8861";settings.dataSource.username = "-";settings.dataSource.password = "-";function syncWithMe(){    alert("Global object 'settings.sync.identifier' set\nfrom xml element's attribute: " + settings.sync.identifier);    var mySyncResult2 = "";    mySyncResult2 = connectToDataSource("//channel");    mySyncResult2 = JSON.parse(mySyncResult2);    mySyncResult2 = mySyncResult2[0].item.description;    return mySyncResult2;}syncWithMe();